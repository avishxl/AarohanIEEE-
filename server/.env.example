# ─────────────────────────────────────────────────────────────
# CyberShield — Server Environment Configuration
# STEP 1: Copy this file → rename to  .env
# STEP 2: Fill in your values below
# ─────────────────────────────────────────────────────────────

PORT=5000
NODE_ENV=development

# Option A: Local MongoDB (run mongod first)
MONGODB_URI=mongodb://localhost:27017/cybershield
# Option B: MongoDB Atlas free tier
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/cybershield

# Change to any random string
JWT_SECRET=cybershield_jwt_secret_replace_me

# Paste your Gemini API key — get free at aistudio.google.com/app/apikey
GEMINI_API_KEY=PASTE_YOUR_KEY_HERE

CLIENT_URL=http://localhost:5173
